/**
 * defines individual items
 * for @{SyndicationFeed.feedItems}
 *
 * @export
 */
export interface SyndicationFeedItem {

    /**
     * title of the item
     */
    title: string;

    /**
     * resource indicator for the item
     */
    link: string;
}
